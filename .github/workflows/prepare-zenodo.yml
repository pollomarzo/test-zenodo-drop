name: Prepare Zenodo deposit

on:
  workflow_dispatch:
    inputs:
      version:
        required: true
        type: string
        description: Release version (e.g. 0.1.4, no leading v)
      sandbox:
        required: false
        type: boolean
        default: true
        description: Use sandbox.zenodo.org

permissions:
  contents: write
  pull-requests: write

jobs:
  prepare:
    uses: impact-scholars/isp-actions-config/.github/workflows/prepare-zenodo.yml@feat/zenodo-publish
    with:
      version: ${{ inputs.version }}
      sandbox: ${{ inputs.sandbox }}
    secrets:
      ZENODO_TOKEN_SANDBOX: ${{ secrets.ZENODO_TOKEN_SANDBOX }}
